#include <iostream>

using namespace std;

int main() {

	int tc;
	cin.tie(0), ios::sync_with_stdio(0);
	cin >> tc;

	for (int t = 0; t < tc; t++) {
		int n, m;
		cin >> n >> m;
		for (int i = 0; i < m; i++) {
			int k;
			cin >> k >> k;
		}
		cout << n - 1 << '\n';
	}

	return 0;
}